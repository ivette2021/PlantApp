<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlantApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PlantApp" parent="Base.Theme.PlantApp" />

    <style name="BottomNavigationStyle" parent="Widget.Design.BottomNavigationView">
        <!-- Cambia el color de fondo del Bottom Navigation -->
        <item name="android:background">@color/PrimaryVariant</item>

        <!-- Cambia el color de los íconos cuando no están seleccionados -->
        <item name="itemIconTint">@color/Primary</item>

        <!-- Cambia el color del texto cuando no está seleccionado -->
        <item name="itemTextColor">@color/PrimaryVariant</item>

    </style>

    <style name="FloatingActionButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <!-- Cambia el color de fondo del FloatingActionButton -->
        <item name="backgroundTint">@color/Secondary</item>

        <!-- Cambia el color del icono del FloatingActionButton -->
        <item name="tint">@color/Error</item>

        <!-- Ajusta el tamaño del FloatingActionButton (opcional) -->
        <item name="fabCustomSize">64dp</item>
    </style>


</resources>